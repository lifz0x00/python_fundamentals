# set must be immmutable types
set_fruits = {'Apple', 'Banana', 'Cherry', 'Durian', True, 7}
print(f"{set_fruits}")

number_pad = {
  (1, 2, 3),
  (4, 5, 6),
  (7, 8, 9),
  (0)
}
print(f"{number_pad}")

# set_list = {77, 88, 99, ['A', 'B']} # set with list -> error
# print(set_list) # error, because list is mutable